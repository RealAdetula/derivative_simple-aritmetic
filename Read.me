# We are going to build a simple calculator that takes two numbers and an operator as input and performs the corresponding operation. The calculator will continue to run until the user decides to exit. We are also going to add some error handling to make sure the user inputs are valid and display a welcome message.